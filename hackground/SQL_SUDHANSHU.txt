24/04/2025


 ALTER TABLE `pref_faq_categories` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

 ALTER TABLE `pref_faq_categories` CHANGE `status` `status` INT(11) NOT NULL DEFAULT '1';

 ALTER TABLE `pref_faq_categories_names` CHANGE `created_at` `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP;

 ALTER TABLE `pref_faq_categories_names` CHANGE `updated_at` `updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP;

ALTER TABLE `pref_faq_list` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

 ALTER TABLE `pref_faq_list` CHANGE `created_at` `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP;

 ALTER TABLE `pref_faq_list` CHANGE `updated_at` `updated_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP;

 ALTER TABLE `pref_faq_list_names` CHANGE `created_at` `created_at` TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP;

 ALTER TABLE `pref_faq_list_names` CHANGE `updated_at` `updated_at` TIMESTAMP on update CURRENT_TIMESTAMP NULL DEFAULT CURRENT_TIMESTAMP;

 ALTER TABLE `pref_faq_list` CHANGE `status` `status` INT(11) NULL DEFAULT '1';



29/04/2025
ALTER TABLE `pref_testimonial` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_email_templates` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_country` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_state` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_city` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_property_furnish` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_property_furnish` CHANGE `icon` `icon` VARCHAR(155) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL;

ALTER TABLE `pref_property_budget` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_property_status` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_property_status` CHANGE `icon` `icon` TEXT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL;

ALTER TABLE `pref_property_transaction` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_property_sub_category` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_project_amenity` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_cms` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_city` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_state` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_country` CHANGE `status` `status` TINYINT(4) NOT NULL DEFAULT '1';

ALTER TABLE `pref_testimonial` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_property_budget` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_property_recommended` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_property_recommended` CHANGE `order` `order` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_property_category` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_property_sub_category` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_property_status` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_property_transaction` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_property_furnish` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_cms` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_email_templates` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';

ALTER TABLE `pref_notification_templates` CHANGE `status` `status` TINYINT(1) NULL DEFAULT '1';

ALTER TABLE `pref_project_amenity` CHANGE `status` `status` TINYINT(1) NOT NULL DEFAULT '1';