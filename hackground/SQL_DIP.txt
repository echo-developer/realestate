
<------------------------------------------------------SOUMYADIP--------------------------------------------------------->


27/01/2025 ---

ALTER TABLE `pref_property_additional` ADD `bhk_type` VARCHAR(255) NULL AFTER `total_flats`;

28/01/2025---

ALTER TABLE `pref_properties` CHANGE `status` `status` INT(11) NOT NULL DEFAULT '0' COMMENT '0 = pending, 1 = published, 2 = draft, -1 = expired';


29/01/2025

ALTER TABLE `pref_project` ADD `is_top` INT NOT NULL DEFAULT '0' AFTER `is_popular`;

ALTER TABLE `pref_properties` ADD `is_top` INT NOT NULL DEFAULT '0' AFTER `is_populer`;

ALTER TABLE `pref_properties_location` ADD `latitude` DECIMAL(10,6) NULL AFTER `property_address`;

ALTER TABLE `pref_properties_location` ADD `longitude` DECIMAL(10,6) NULL AFTER `latitude`;

ALTER TABLE `pref_property_additional` ADD `bhk_id` VARCHAR(255) NULL DEFAULT NULL AFTER `total_flats`;

31/01/2025--------

CREATE TABLE `project_property_mapping` (
  `id` int(20) NOT NULL AUTO_INCREMENT,
  `project_id` int(20) NOT NULL,
  `tower_id` int(20) NOT NULL,
  `property_id` int(20) NOT NULL,
  `created_at` timestamp NOT NULL DEFAULT current_timestamp(),
  `updated_at` timestamp NOT NULL DEFAULT current_timestamp() ON UPDATE current_timestamp()
)

ALTER TABLE `pref_project_gallery_images` ADD `id` INT NOT NULL AUTO_INCREMENT FIRST, ADD PRIMARY KEY (`id`);
ALTER TABLE `project_floors` ADD `flat_no` INT NOT NULL AFTER `floor_no`;
ALTER TABLE `project_property_mapping` ADD `floor_id` INT NOT NULL DEFAULT '0' AFTER `tower_id`;

ALTER TABLE `pref_project_location` ADD `latitude` DECIMAL(10,6) NULL AFTER `address`;

ALTER TABLE `pref_project_location` ADD `longitude` DECIMAL(10,6) NULL AFTER `latitude`;

ALTER TABLE `pref_floor_plan_type_names` CHANGE `fp_id` `fpt_id` INT(11) NULL DEFAULT NULL;

ALTER TABLE `pref_floor_plan`
  DROP `project_id`;


  12/02/2025

  ALTER TABLE `pref_floor_plan_type_names` CHANGE `fp_id` `fpt_id` INT(11) NULL DEFAULT NULL;

  RENAME TABLE membership_features TO pref_membership_features,
              membership_features_names TO pref_membership_features_names,
              membership_plans TO pref_membership_plans,
              membership_plan_names TO pref_membership_plan_names;

              RENAME TABLE otps TO pref_otps


              ALTER TABLE `agents_rating` RENAME `pref_agents_rating`; ALTER TABLE `agent_social_platforms` RENAME `pref_agent_social_platforms`; ALTER TABLE `buyer_agent_enquiry` RENAME `pref_buyer_agent_enquiry`;
               ALTER TABLE `buyer_property_enquery` RENAME `pref_buyer_property_enquery`; ALTER TABLE `email_verify_otp` RENAME `pref_email_verify_otp`;

              ALTER TABLE `agent_service_location` RENAME `pref_agent_service_location`; ALTER TABLE `membership_plan_features` RENAME `pref_membership_plan_features`; ALTER TABLE `project_floors` RENAME `pref_project_floors`; ALTER TABLE `project_properties` RENAME `pref_project_properties`; ALTER TABLE `project_property_mapping` RENAME `pref_project_property_mapping`; ALTER TABLE `project_review_additional` RENAME `pref_project_review_additional`; ALTER TABLE `project_views` RENAME `pref_project_views`; ALTER TABLE `property_review_additional` RENAME `pref_property_review_additional`; ALTER TABLE `property_views` RENAME `pref_property_views`; ALTER TABLE `transactions` RENAME `pref_transactions`;


              ALTER TABLE `pref_property_status` ADD `icon` TEXT NOT NULL AFTER `id`;

 CREATE TABLE pref_membership_plan_type (
                  id INT AUTO_INCREMENT PRIMARY KEY,
                  no_of_owners_contactable INT NOT NULL,
                  unlock_owner_properties TINYINT(1) NOT NULL DEFAULT 0,
                  assistance_relationship_manager TINYINT(1) NOT NULL DEFAULT 0,
                  early_access_days INT NOT NULL DEFAULT 3,
                  validity_days INT NOT NULL,
                  prime_tag TINYINT(1) NOT NULL DEFAULT 0,
                  home_guarantee_refund TINYINT(1) NOT NULL DEFAULT 0,
                  status TINYINT(1) NOT NULL DEFAULT 1
              );

 ALTER TABLE `pref_property_furnish` ADD `icon` VARCHAR(155) NOT NULL AFTER `id`; 

 about plan add iiin plan
 remove slug add plain type_id

 ALTER TABLE `pref_membership_plans` ADD `plan_type_id` INT NOT NULL AFTER `validity_days`;
----3-31--25----
ALTER TABLE `pref_project_properties` CHANGE `bhk_id` `slug` VARCHAR(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL;
ALTER TABLE `pref_property_additional` ADD `slug` VARCHAR(255) NOT NULL AFTER `bhk_type`;
ALTER TABLE `pref_user_membership` CHANGE `remaining_leads` `leads_used` INT(13) NULL DEFAULT '0';
ALTER TABLE `pref_area_locality_price` ADD `price_for` VARCHAR(155) NULL AFTER `new_price`;

---------------------------04/24/2025---------------
-- phpMyAdmin SQL Dump
-- version 5.2.1
-- https://www.phpmyadmin.net/
--
-- Host: 127.0.0.1
-- Generation Time: Apr 24, 2025 at 12:21 PM
-- Server version: 10.4.32-MariaDB
-- PHP Version: 8.2.12

SET SQL_MODE = "NO_AUTO_VALUE_ON_ZERO";
START TRANSACTION;
SET time_zone = "+00:00";

--
-- Database: `realestate`
--

-- --------------------------------------------------------

--
-- Table structure for table `pref_area_locality_price`
--

CREATE TABLE `pref_area_locality_price` (
  `id` int(11) NOT NULL,
  `locality` int(11) DEFAULT NULL,
  `year` varchar(155) DEFAULT NULL,
  `price_per_sqft` decimal(12,2) DEFAULT NULL,
  `new_price` decimal(12,2) DEFAULT NULL,
  `price_for` varchar(155) DEFAULT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

--
-- Dumping data for table `pref_area_locality_price`
--

INSERT INTO `pref_area_locality_price` (`id`, `locality`, `year`, `price_per_sqft`, `new_price`, `price_for`) VALUES
(1, 3, '2025', 6164.78, NULL, 'prop'),
(2, 2, '2025', 216.49, NULL, 'proj'),
(3, 1, '2025', 6.12, NULL, 'proj');

--
-- Indexes for dumped tables
--

--
-- Indexes for table `pref_area_locality_price`
--
ALTER TABLE `pref_area_locality_price`
  ADD PRIMARY KEY (`id`);

--
-- AUTO_INCREMENT for dumped tables
--

--
-- AUTO_INCREMENT for table `pref_area_locality_price`
--
ALTER TABLE `pref_area_locality_price`
  MODIFY `id` int(11) NOT NULL AUTO_INCREMENT, AUTO_INCREMENT=4;
COMMIT;


ALTER TABLE `pref_agent_additional_details` ADD `company_logo` TEXT NULL DEFAULT NULL AFTER `company_name`;
ALTER TABLE `pref_agent_additional_details` ADD `language_speak` VARCHAR(255) NULL AFTER `specialization`;
ALTER TABLE `pref_agent_additional_details` ADD `agent_cover_photo` VARCHAR(255) NULL AFTER `company_logo`;
ALTER TABLE `pref_property_additional` ADD `property_video` VARCHAR(255) NULL AFTER `brochure_file`;